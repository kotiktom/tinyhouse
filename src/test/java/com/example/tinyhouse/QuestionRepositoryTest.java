package com.example.tinyhouse;

/*
 * @RunWith(SpringRunner.class)
 * 
 * @DataJpaTest public class QuestionRepositoryTest {
 * 
 * @Autowired private QuestionRepository repository;
 * 
 * @Test public void findQuestion() { List<Question> questions =
 * repository.findByQuestion("test1");
 * 
 * assertThat(questions).hasSize(1);
 * assertThat(questions.get(0)).isEqualTo("test1"); }
 * 
 * }
 */
